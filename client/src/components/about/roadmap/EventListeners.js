import React from "react";

const EventListeners = ({ eventOneRef }) => {
  return (
    <div id="section-8" ref={eventOneRef}>
      EventListeners
    </div>
  );
};

export default EventListeners;
